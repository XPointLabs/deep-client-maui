{
  "schemaVersion": "survival-migration-rollback-v1",
  "migrationRequired": false,
  "migration": [
    "Generate one lowercase 32-hex release invocation and pass it to every strict lane and the final validator.",
    "Build Android Debug physical E2E with DeepPhysicalE2E=true and select only network.xpoint.deep.e2e.",
    "Mr. X copies eng/policies/android-lab-policy.template.json into .secrets/android-lab, provisions exact commit/tool/device/APK values plus an approval receipt, and passes that protected path to both wrapper and validator.",
    "Provide a repository-local evidence directory and an isolated per-run app-data root.",
    "Use adb reverse for a physical Android test device instead of weakening release cleartext policy."
  ],
  "dataMigration": "none",
  "rollback": [
    "Stop promotion and preserve rejected evidence locally for analysis.",
    "Ship a reviewed forward corrective commit; do not restore hash-only Windows checks, caller Android self-attestation, synthetic release evidence, weaker JUnit privacy filtering, or raw artifact uploads."
  ],
  "rollbackRisk": "Reverting this hardening restores transient payload substitution, untrusted Android tool/device claims, malformed APK acceptance, and private Android artifact disclosure; rollback is forward-only."
}
